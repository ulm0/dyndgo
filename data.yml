credentials:
  token: token
zones:
  # Domain must be A records only
  domain.com:
    updateRootDomain: true # update root domain as well (domain.com in this case)
    recordsList:
      - subdomain1
      - subdomain2